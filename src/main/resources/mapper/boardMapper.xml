<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-/mybatis.org//DTD mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.soo.routine.mapper.BoardMapper">

	<sql id="maxNum">
		select nvl(max(boardNum), 0) + 1 from board
	</sql>
	
	<select id="getList" resultType="com.soo.routine.domain.Board" parameterType="String">
		select * from board where categoryName LIKE '%' || #{categoryName} || '%'
	</select>

	<insert id="createData" parameterType="com.soo.routine.domain.Board">
		insert into board(boardNum, memberNum, categoryName, boardTitle, boardContent, boardCreate, boardModify, boardHits)
			values((<include refid="maxNum"/>), #{memberNum}, #{categoryName}, #{boardTitle}, #{boardContent}, now(), null, 0)
	</insert>

	<select id="getData" resultType="com.soo.routine.domain.Board" parameterType="int">
		select * from board where boardNum = #{boardNum}
	</select>




</mapper>
